# Managed by Blink — do not edit manually (service: test-demo)
version: '3.8'

services:
  app:
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: "${NODE_ENV:-development}"
    restart: unless-stopped